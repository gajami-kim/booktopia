<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.booktopia.www.repository.ChatBotMapper">

    <select id="getcahtBot" resultType="com.booktopia.www.domain.ChatBotVO">
        select * from chatbot where chat_no=#{chatNO}
    </select>
</mapper>